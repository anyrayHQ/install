AWSTemplateFormatVersion: '2010-09-09'

# AWS::LanguageExtensions enables Fn::ForEach, used below to fan the ALB
# ingress rules out over the (comma-separated) list of AllowedCidr values.
Transform: AWS::LanguageExtensions

Description: >-
  Anyray one-click deploy (ECS/Fargate). Brings up the full self-hosted stack as
  managed containers — gateway + optimizer + console proxy on AWS Fargate, with a
  managed RDS PostgreSQL store and EFS for runtime config. An Application Load
  Balancer fronts the gateway API (:8787) and the console (:3000), scoped to the
  CIDR you provide (never 0.0.0.0/0). All secrets are generated by CloudFormation
  into Secrets Manager — no bootstrap script. Content stays encrypted at rest;
  only metadata is logged.

Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label: { default: Start here }
        Parameters: [AllowedCidr, DeploymentToken]
      - Label: { default: Images }
        Parameters: [ImageTag, EnableAutoUpdate, DefaultModel]
      - Label: { default: Bedrock (Claude and other models from your own AWS account) }
        Parameters: [EnableBedrock]
      - Label: { default: Ingress / TLS }
        Parameters: [LoadBalancerScheme, CertificateArn, GatewayPublicUrl, ConsolePublicUrl]
      - Label: { default: Anyray Cloud (metering) }
        Parameters: [SeatExclude, MeteringIntervalMs, EntitlementGraceMs]
      - Label: { default: Content privacy }
        Parameters: [ContentMode, AllowPlaintext, SpendRetentionDays]
      - Label: { default: Gateway hardening }
        Parameters:
          [
            Hsts,
            RateLimitRpm,
            RateLimitIpRpm,
            RateLimitUnauthRpm,
            MaxConcurrentRequests,
            AuthFailLimit,
            MaxBodyBytes,
            CustomHostAllowlist,
            TrustedCustomHosts,
            UpstreamUrl,
          ]
      - Label: { default: Optimizer }
        Parameters: [OptimizerTimeoutMs, OptimizerVisionTimeoutMs]
      - Label: { default: Availability }
        Parameters: [HighAvailability]
      - Label: { default: Database }
        Parameters: [DbInstanceClass, DbAllocatedStorage, DbMaxAllocatedStorage, EnableRdsDeletionProtection]
      - Label: { default: Advanced networking (leave blank) }
        Parameters: [VpcId, SubnetA, SubnetB]
    ParameterLabels:
      AllowedCidr: { default: "Allowed CIDR(s) (your office/VPN range, comma-separated)" }
      LoadBalancerScheme: { default: "Load balancer reachability (internal = private, VPC-only)" }
      EnableRdsDeletionProtection: { default: "Protect the database from deletion (production)" }
      HighAvailability: { default: "Run redundant tasks and a Multi-AZ database (production)" }
      EnableBedrock: { default: "Let the gateway call Bedrock with this stack's own role (no access keys)" }
      VpcId: { default: "VPC (leave blank = account default VPC)" }
      SubnetA: { default: "Public subnet in AZ 1 (leave blank)" }
      SubnetB: { default: "Public subnet in AZ 2 (leave blank)" }

Parameters:
  VpcId:
    Type: String
    Default: ''
    AllowedPattern: '^$|^vpc-[0-9a-f]{8,17}$'
    ConstraintDescription: Leave blank, or give a VPC id like vpc-0a1b2c3d.
    Description: >-
      LEAVE BLANK to use this account's default VPC — that is the normal choice
      and needs no networking knowledge. Only set this (together with SubnetA
      and SubnetB) to place Anyray in a specific VPC.
  SubnetA:
    Type: String
    Default: ''
    AllowedPattern: '^$|^subnet-[0-9a-f]{8,17}$'
    ConstraintDescription: Leave blank, or give a subnet id like subnet-0a1b2c3d.
    Description: >-
      Leave blank unless you set VpcId. A PUBLIC subnet that auto-assigns a
      public IPv4 — tasks pull images from GHCR and reach the Portal/providers
      directly (no NAT gateway). Validated before anything is created.
  SubnetB:
    Type: String
    Default: ''
    AllowedPattern: '^$|^subnet-[0-9a-f]{8,17}$'
    ConstraintDescription: Leave blank, or give a subnet id like subnet-0a1b2c3d.
    Description: >-
      Leave blank unless you set VpcId. A second PUBLIC subnet in a DIFFERENT AZ
      — the load balancer and RDS both require two AZs.
  AllowedCidr:
    Type: CommaDelimitedList
    Description: >-
      CIDR range(s) allowed to reach the console (:3000) and gateway (:8787)
      through the load balancer. Scope this to your office / VPN range(s). Pass a
      single CIDR, or several comma-separated (e.g.
      203.0.113.0/24,198.51.100.0/24). Do NOT use 0.0.0.0/0.
    AllowedPattern: '^(\d{1,3}\.){3}\d{1,3}/\d{1,2}$'
    ConstraintDescription: >-
      Each value must be a CIDR like 203.0.113.0/24 (not 0.0.0.0/0); separate
      multiple with commas.
  ImageTag:
    Type: String
    Default: v1.10.317
    AllowedPattern: '^v[0-9]+\.[0-9]+\.[0-9]+$'
    ConstraintDescription: Must be an immutable Anyray release tag such as v1.10.120.
    Description: >-
      Immutable Anyray release tag deployed by every app service. This default
      is stamped by the release workflow; mutable latest/stable channels are not
      accepted because independently rolled ECS tasks must stay compatible.
      Minimum v1.10.246 — the release that first published the endpoint-control
      image to the customer set. Updating an older stack onto this template with
      UsePreviousValue keeps a tag that image has no manifest for, so the new
      service never stabilises and CloudFormation rolls the whole update back;
      pass the current tag instead.
  EnableAutoUpdate:
    Type: String
    Default: 'true'
    AllowedValues: ['true', 'false']
    Description: >-
      Let this deployment apply compatible ("soft") releases on its own. The
      gateway already decides WHETHER an update is safe — it evaluates the
      release manifest against this install and never applies one needing
      operator action. This only gives it the arm it lacks on ECS: a Lambda
      that rolls every app service together. Turning it off removes that arm;
      the gateway keeps notifying in the console either way. The operator can
      also flip it at runtime via PUT /admin/update/settings.
  EnableBedrock:
    Type: String
    Default: 'false'
    AllowedValues: ['true', 'false']
    Description: >-
      Let the gateway serve AWS Bedrock using THIS stack's task role, so no
      access key is stored anywhere: configure the bedrock provider in the
      console with a region and both key fields left blank. Off by default
      because it grants model-invoke permission, which a stack that never uses
      Bedrock has no reason to hold. The two List actions it also grants are
      what lets the gateway resolve a Claude model id to the id Bedrock serves
      in your region; without them only the model ids built into the gateway
      resolve, and anything newer fails. Safe to turn on later — it is an IAM
      change, and no service is replaced.
  DefaultModel:
    Type: String
    Default: 'anthropic/claude-sonnet-4-5'
    Description: >-
      Model the `anyray-default` routing sentinel resolves to. Set it to a model
      your configured provider serves (a bare model is read under `anthropic`).
  LoadBalancerScheme:
    Type: String
    Default: internet-facing
    AllowedValues: [internet-facing, internal]
    Description: >-
      Whether the load balancer gets public IPs. Choose "internal" to keep Anyray
      entirely inside your VPC, reachable only over VPN, Direct Connect, or a peered
      network - its DNS name then resolves to private addresses and there is no
      public entry point at all. The ECS tasks keep their own egress either way, so
      the gateway still reaches your model providers. With "internal" also set
      AllowedCidr to the ranges that should reach it (your VPC or corporate CIDR,
      not 0.0.0.0/0), and expect the ConsoleURL and GatewayURL outputs to be
      resolvable only from inside the network.
  CertificateArn:
    Type: String
    Default: ''
    AllowedPattern: '^$|^arn:aws:acm:.+$'
    Description: >-
      Optional ACM certificate ARN. If set, both listeners serve HTTPS; if empty,
      they serve HTTP (scoped to AllowedCidr).
  GatewayPublicUrl:
    Type: String
    Default: ''
    AllowedPattern: '^$|^https?://\S+$'
    Description: Optional externally reachable gateway URL (defaults to the load balancer DNS).
  ConsolePublicUrl:
    Type: String
    Default: ''
    AllowedPattern: '^$|^https?://\S+$'
    Description: Optional externally reachable console URL (defaults to the load balancer DNS).
  DeploymentToken:
    Type: String
    NoEcho: true
    AllowedPattern: '^adt_[A-Za-z0-9_-]+$'
    Description: Anyray Cloud deployment token (adt_...) for usage metering.
  SeatExclude:
    Type: String
    Default: ''
    Description: >-
      Optional comma-separated globs of user ids to exclude from the billed
      seat count (CI bots, service accounts), e.g. "bot-*,ci-*". Empty bills
      every active user.
  MeteringIntervalMs:
    Type: String
    Default: ''
    AllowedPattern: '^$|^[0-9]+$'
    Description: Optional metering rollup upload interval in milliseconds (empty = gateway default).
  EntitlementGraceMs:
    Type: String
    Default: ''
    AllowedPattern: '^$|^[0-9]+$'
    Description: >-
      Optional grace window in milliseconds the gateway keeps serving after the
      entitlement lease expires (empty = gateway default).
  ContentMode:
    Type: String
    Default: encrypted
    AllowedValues: ['encrypted', 'off', 'plaintext']
    Description: Content-privacy mode. `encrypted` (default) keeps content as AES-256-GCM ciphertext.
  AllowPlaintext:
    Type: String
    Default: 'false'
    AllowedValues: ['true', 'false']
    Description: Deploy gate that must be true before `plaintext` content mode can be selected.
  Hsts:
    Type: String
    Default: 'false'
    AllowedValues: ['true', 'false']
    Description: Emit HSTS headers (set true only when served over HTTPS).
  SpendRetentionDays:
    Type: String
    Default: ''
    AllowedPattern: '^$|^[0-9]+$'
    Description: >-
      Retention window in days. Prunes spend rows older than this (empty =
      keep forever) and overrides the trace-store window (traces default to
      90 days when empty).
  RateLimitRpm:
    Type: String
    Default: ''
    AllowedPattern: '^$|^[0-9]+$'
    Description: Optional per-identity /v1 request limit per minute.
  RateLimitIpRpm:
    Type: String
    Default: ''
    AllowedPattern: '^$|^[0-9]+$'
    Description: Optional per-source-IP /v1 request limit per minute.
  RateLimitUnauthRpm:
    Type: String
    Default: ''
    AllowedPattern: '^$|^[0-9]+$'
    Description: Optional unauthenticated endpoint request limit per minute.
  MaxConcurrentRequests:
    Type: String
    Default: ''
    AllowedPattern: '^$|^[0-9]+$'
    Description: Optional max concurrent /v1 requests per identity or IP.
  AuthFailLimit:
    Type: String
    Default: ''
    AllowedPattern: '^$|^[0-9]+$'
    Description: Optional max failed auth attempts per identity/IP before a temporary lockout.
  MaxBodyBytes:
    Type: String
    Default: ''
    AllowedPattern: '^$|^[0-9]+$'
    Description: Optional max request body size in bytes.
  CustomHostAllowlist:
    Type: String
    Default: ''
    Description: >-
      Optional comma-separated hostnames (no port) allowed as custom provider
      hosts — set together with TrustedCustomHosts when UpstreamUrl points at an
      internal host, so it clears the SSRF proxy guard.
  TrustedCustomHosts:
    Type: String
    Default: ''
    Description: >-
      Optional comma-separated custom hosts trusted by the SSRF proxy guard
      (pair with CustomHostAllowlist).
  UpstreamUrl:
    Type: String
    Default: ''
    AllowedPattern: '^$|^https?://\S+$'
    Description: >-
      On-top mode: optional OpenAI-compatible upstream gateway (LiteLLM,
      OpenRouter, ...) that seeds the default routing config, so clients send no
      x-anyray-config header. Empty = route directly to providers.
  OptimizerTimeoutMs:
    Type: String
    Default: '800'
    AllowedPattern: '^[0-9]+$'
    Description: Optimizer timeout for normal requests in milliseconds.
  OptimizerVisionTimeoutMs:
    Type: String
    Default: '10000'
    AllowedPattern: '^[0-9]+$'
    Description: Optimizer timeout for vision requests in milliseconds.
  DbInstanceClass:
    Type: String
    Default: db.t4g.micro
    Description: RDS PostgreSQL instance class.
  DbAllocatedStorage:
    Type: Number
    Default: 50
    MinValue: 20
    Description: >-
      RDS allocated storage in GiB. The gateway keeps 90 days of trace content
      by default, so the database grows for its first three months before the
      first prune reclaims anything; 50 GiB holds that window for a typical
      team. Heavy whole-team + CI usage can add ~1 GB/day, which storage
      autoscaling absorbs up to DbMaxAllocatedStorage.
  DbMaxAllocatedStorage:
    Type: Number
    Default: 100
    MinValue: 21
    Description: >-
      Upper bound for RDS storage autoscaling in GiB (must exceed allocated
      storage). Trace content accumulates until the 90-day retention window
      fills, so the database grows for its first three months; without
      autoscaling it goes storage-full and stops accepting writes.
  HighAvailability:
    Type: String
    Default: 'false'
    AllowedValues: ['true', 'false']
    Description: >-
      Remove the single points of failure. Off, this stack runs ONE Fargate task
      per service and a single-AZ database, so any task or AZ loss is a full
      outage until AWS replaces it (typically minutes) — fine for an evaluation,
      not for traffic anyone depends on. On, each service runs two tasks spread
      across the stack's two AZs and the database becomes Multi-AZ with automatic
      failover. Both halves matter: redundant tasks in front of a single-AZ
      database still go down with that AZ. This roughly doubles the compute and
      database cost of the stack, which is why it is off by default. Safe to turn
      on later — it is a rolling change, not a rebuild.
  EnableRdsDeletionProtection:
    Type: String
    Default: 'false'
    AllowedValues: ['true', 'false']
    Description: >-
      Block deletion of the database instance. Recommended for production: it
      guards against an accidental console or CLI delete, which DeletionPolicy
      Snapshot does not cover (that only applies to deletion through this
      stack). Left off by default so the stack stays one-click removable. NOTE:
      while this is true, deleting the stack FAILS — set it back to false and
      update the stack first, then delete.

Rules:
  AllowedCidrMustNotBePublic:
    Assertions:
      - Assert:
          Fn::Not:
            - Fn::Contains:
                - !Ref AllowedCidr
                - '0.0.0.0/0'
        AssertDescription: AllowedCidr must be scoped to your org or VPN, not 0.0.0.0/0.
  VpcTrioAllOrNothing:
    Assertions:
      - Assert:
          Fn::Or:
            - Fn::And:
                - Fn::Equals: [!Ref VpcId, '']
                - Fn::Equals: [!Ref SubnetA, '']
                - Fn::Equals: [!Ref SubnetB, '']
            - Fn::And:
                - Fn::Not: [Fn::Equals: [!Ref VpcId, '']]
                - Fn::Not: [Fn::Equals: [!Ref SubnetA, '']]
                - Fn::Not: [Fn::Equals: [!Ref SubnetB, '']]
        AssertDescription: >-
          Leave VpcId, SubnetA and SubnetB ALL blank to use this account's default
          VPC, or set all three. Setting only some of them is not valid.

Conditions:
  HasCert: !Not [!Equals [!Ref CertificateArn, '']]
  HasGatewayUrl: !Not [!Equals [!Ref GatewayPublicUrl, '']]
  HasConsoleUrl: !Not [!Equals [!Ref ConsolePublicUrl, '']]
  RdsDeletionProtection: !Equals [!Ref EnableRdsDeletionProtection, 'true']
  AutoUpdate: !Equals [!Ref EnableAutoUpdate, 'true']
  InternalLb: !Equals [!Ref LoadBalancerScheme, 'internal']
  Ha: !Equals [!Ref HighAvailability, 'true']
  Bedrock: !Equals [!Ref EnableBedrock, 'true']

Resources:
  # ---- Network resolution -----------------------------------------------------
  # CloudFormation cannot discover the default VPC, so a tiny read-only Lambda
  # does it. It ALWAYS runs: given blank inputs it picks the default VPC and two
  # public subnets in different AZs; given explicit ones it validates them and
  # echoes them back. That turns the classic failure of this template — a private
  # or same-AZ subnet blowing up halfway through with an opaque ECS error — into
  # an immediate, readable message, and lets a non-DevOps user deploy with no
  # networking input at all.
  NetFnRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: describe-network
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              # Describe* does not support resource-level permissions.
              - Effect: Allow
                Action: [ec2:DescribeVpcs, ec2:DescribeSubnets, ec2:DescribeRouteTables]
                Resource: '*'

  NetFn:
    Type: AWS::Lambda::Function
    Properties:
      Runtime: python3.12
      Handler: index.handler
      Timeout: 60
      Role: !GetAtt NetFnRole.Arn
      Code:
        ZipFile: |
          import json, urllib.request, traceback, boto3
          ec2 = boto3.client('ec2')

          def send(event, context, status, data=None, reason=None):
              body = json.dumps({
                  'Status': status,
                  'Reason': reason or ('See CloudWatch log stream ' + context.log_stream_name),
                  'PhysicalResourceId': event.get('PhysicalResourceId') or context.log_stream_name,
                  'StackId': event['StackId'], 'RequestId': event['RequestId'],
                  'LogicalResourceId': event['LogicalResourceId'],
                  'Data': data or {},
              }).encode()
              req = urllib.request.Request(event['ResponseURL'], data=body, method='PUT',
                  headers={'content-type': '', 'content-length': str(len(body))})
              urllib.request.urlopen(req)

          def routes_to_igw(subnet_id, vpc):
              # MapPublicIpOnLaunch only assigns an address; without a default
              # route to an internet gateway the task still cannot reach GHCR.
              rts = ec2.describe_route_tables(Filters=[
                  {'Name': 'association.subnet-id', 'Values': [subnet_id]}])['RouteTables']
              if not rts:
                  # No explicit association means the subnet uses the VPC main table.
                  rts = ec2.describe_route_tables(Filters=[
                      {'Name': 'vpc-id', 'Values': [vpc]},
                      {'Name': 'association.main', 'Values': ['true']}])['RouteTables']
              for rt in rts:
                  for r in rt.get('Routes', []):
                      if (r.get('DestinationCidrBlock') == '0.0.0.0/0'
                              and (r.get('GatewayId') or '').startswith('igw-')):
                          return True
              return False

          def resolve(props):
              vpc = (props.get('VpcId') or '').strip()
              a = (props.get('SubnetA') or '').strip()
              b = (props.get('SubnetB') or '').strip()
              given = [x for x in (vpc, a, b) if x]

              if given and len(given) != 3:
                  raise Exception(
                      'Set all three of VpcId, SubnetA and SubnetB, or leave all three '
                      'blank to use this account default VPC.')

              if not given:
                  vpcs = ec2.describe_vpcs(
                      Filters=[{'Name': 'isDefault', 'Values': ['true']}])['Vpcs']
                  if not vpcs:
                      raise Exception(
                          'This account has no default VPC in this region. Either create one '
                          '(VPC console > Actions > Create default VPC) or set VpcId, SubnetA '
                          'and SubnetB explicitly.')
                  vpc = vpcs[0]['VpcId']
                  subs = ec2.describe_subnets(Filters=[
                      {'Name': 'vpc-id', 'Values': [vpc]},
                      {'Name': 'default-for-az', 'Values': ['true']}])['Subnets']
                  by_az = {}
                  for sn in sorted(subs, key=lambda x: x['SubnetId']):
                      if (sn.get('MapPublicIpOnLaunch')
                              and routes_to_igw(sn['SubnetId'], vpc)):
                          by_az.setdefault(sn['AvailabilityZone'], sn['SubnetId'])
                  if len(by_az) < 2:
                      raise Exception(
                          'The default VPC has public subnets in fewer than two availability '
                          'zones, but the load balancer and RDS both need two. Set SubnetA and '
                          'SubnetB explicitly.')
                  azs = sorted(by_az)
                  a, b = by_az[azs[0]], by_az[azs[1]]
              else:
                  # Check this before DescribeSubnets: the API de-duplicates ids, so
                  # passing the same subnet twice comes back as a single result and
                  # would slip past the availability-zone comparison below.
                  if a == b:
                      raise Exception(
                          'SubnetA and SubnetB are the same subnet (%s). The load balancer '
                          'and RDS both need two subnets in different availability zones.' % a)
                  found = ec2.describe_subnets(SubnetIds=[a, b])['Subnets']
                  for sn in found:
                      if sn['VpcId'] != vpc:
                          raise Exception('Subnet %s is not in %s.' % (sn['SubnetId'], vpc))
                      if not sn.get('MapPublicIpOnLaunch'):
                          raise Exception(
                              'Subnet %s does not auto-assign a public IPv4. Tasks pull images '
                              'from GHCR with no NAT gateway, so both subnets must be public.'
                              % sn['SubnetId'])
                      if not routes_to_igw(sn['SubnetId'], vpc):
                          raise Exception(
                              'Subnet %s has no default route to an internet gateway, so tasks '
                              'in it cannot reach GHCR to pull images. Use a public subnet.'
                              % sn['SubnetId'])
                  azs = {sn['AvailabilityZone'] for sn in found}
                  if len(azs) < 2:
                      raise Exception(
                          'SubnetA and SubnetB must be in different availability zones (both '
                          'are in %s).' % found[0]['AvailabilityZone'])

              return {'VpcId': vpc, 'SubnetA': a, 'SubnetB': b}

          def handler(event, context):
              try:
                  if event['RequestType'] == 'Delete':
                      return send(event, context, 'SUCCESS')
                  return send(event, context, 'SUCCESS', resolve(event['ResourceProperties']))
              except Exception as e:
                  print('NetFn FAILED:', traceback.format_exc())
                  return send(event, context, 'FAILED', None, str(e))

  Net:
    Type: Custom::AnyrayNetwork
    Properties:
      ServiceToken: !GetAtt NetFn.Arn
      VpcId: !Ref VpcId
      SubnetA: !Ref SubnetA
      SubnetB: !Ref SubnetB

  # ---- Secret generation (no setup.sh) --------------------------------------
  SecretFnRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: anyray-secret-gen
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - secretsmanager:CreateSecret
                  - secretsmanager:PutSecretValue
                  - secretsmanager:DescribeSecret
                  - secretsmanager:GetSecretValue
                  - secretsmanager:DeleteSecret
                  - secretsmanager:TagResource
                Resource: !Sub 'arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:${AWS::StackName}/anyray/*'
              # The master credential is now <stack>/anyray/db-master, already
              # covered by the prefixed grant above — no separate statement, and
              # no read of an RDS-managed rds!db-… secret any more.

  SecretFn:
    Type: AWS::Lambda::Function
    Properties:
      Runtime: python3.12
      Handler: index.handler
      Timeout: 60
      Role: !GetAtt SecretFnRole.Arn
      Code:
        ZipFile: |
          import json, secrets as rnd, urllib.request, urllib.parse, traceback, boto3
          sm = boto3.client('secretsmanager')

          def send(event, context, status, data=None, pid=None):
              body = json.dumps({
                  'Status': status,
                  'Reason': 'See CloudWatch log stream ' + context.log_stream_name,
                  'PhysicalResourceId': pid or event.get('PhysicalResourceId') or context.log_stream_name,
                  'StackId': event['StackId'], 'RequestId': event['RequestId'],
                  'LogicalResourceId': event['LogicalResourceId'],
                  'Data': data or {},
              }).encode()
              req = urllib.request.Request(event['ResponseURL'], data=body, method='PUT',
                  headers={'content-type': '', 'content-length': str(len(body))})
              urllib.request.urlopen(req)

          def ensure(name, gen):
              # create-if-missing; never overwrite (content key / salt must be stable)
              try:
                  return sm.create_secret(Name=name, SecretString=gen())['ARN']
              except sm.exceptions.ResourceExistsException:
                  return sm.describe_secret(SecretId=name)['ARN']

          def put(name, value):
              try:
                  return sm.create_secret(Name=name, SecretString=value)['ARN']
              except sm.exceptions.ResourceExistsException:
                  sm.put_secret_value(SecretId=name, SecretString=value)
                  return sm.describe_secret(SecretId=name)['ARN']

          def compose_db_url(props):
              m = json.loads(sm.get_secret_value(SecretId=props['MasterSecretArn'])['SecretString'])
              # URL-encode credentials: the RDS-managed master password can
              # contain URL-reserved chars (?, #, /, @, %, …) that otherwise
              # corrupt the postgresql:// connection string ("Invalid URL").
              # sslmode=no-verify: RDS (PG15+) forces SSL via rds.force_ssl, and
              # the gateway's pg client has no RDS CA bundle — encrypt in transit
              # without cert verification (the DB is private, in-VPC only).
              q = lambda s: urllib.parse.quote(s, safe='')
              return 'postgresql://%s:%s@%s:5432/%s?sslmode=no-verify' % (
                  q(m['username']), q(m['password']), props['DbHost'], props['DbName'])

          def refresh_db_url(props):
              # Re-derive db-url from whatever the master secret holds NOW.
              #
              # The stack no longer rotates that password (the Db resource sets
              # MasterUserPassword from <stack>/anyray/db-master instead of
              # ManageMasterUserPassword), so on a current stack this is a
              # no-op in the steady state. It stays because it is the
              # convergence path for the two cases that still diverge: a stack
              # created before that change, whose RDS-managed password rotated
              # while its db-url did not, and an operator who deliberately
              # rotates db-master by hand. Both then heal on the next hourly
              # tick or the next roll, rather than surfacing as a crash-loop on
              # some later fresh connection.
              name = props['Prefix'] + 'db-url'
              # Follow an existing secret; never create one. The composer owns
              # creation. If db-url is absent the stack is either not up yet or
              # is being torn down (the Delete branch force-deletes it), and a
              # refresh that raced in behind that delete would resurrect a
              # live master credential as an orphan outliving its own stack.
              try:
                  current = sm.get_secret_value(SecretId=name)['SecretString']
              except (sm.exceptions.ResourceNotFoundException,
                      sm.exceptions.InvalidRequestException):
                  return {'Changed': False, 'Reason': 'absent'}
              url = compose_db_url(props)
              if current == url:
                  return {'Changed': False}
              sm.put_secret_value(SecretId=name, SecretString=url)
              # Never log the URL itself: it carries the master password.
              print('db-url recomposed from the rotated master secret')
              return {'Changed': True}

          def handler(event, context):
              # Two callers, two contracts. CloudFormation sends RequestType and
              # a ResponseURL to answer; the schedule rule sends the properties
              # bare and must never try to PUT to a response URL that is not
              # there — including from the failure path, which is why this
              # returns before the CFN try/except below.
              if 'RequestType' not in event:
                  try:
                      return refresh_db_url(event['ResourceProperties'])
                  except Exception:
                      # Raise: a silent refresh failure is a stack that looks
                      # protected and is not. This surfaces on the function's
                      # error metric.
                      print('SecretFn refresh FAILED:', traceback.format_exc())
                      raise
              try:
                  rt = event['RequestType']; props = event['ResourceProperties']
                  action = props['Action']; prefix = props['Prefix']
                  if rt == 'Delete':
                      for n in props.get('Names', []):
                          try:
                              sm.delete_secret(SecretId=prefix + n, ForceDeleteWithoutRecovery=True)
                          except Exception:
                              pass
                      return send(event, context, 'SUCCESS', {}, prefix + action)
                  if action == 'generate':
                      data = {
                          'ContentKeyArn': ensure(prefix + 'content-key', lambda: rnd.token_hex(32)),
                          'OptimizerTokenArn': ensure(prefix + 'optimizer-token', lambda: rnd.token_hex(32)),
                          'AdminTokenArn': ensure(prefix + 'admin-token', lambda: 'ar-adm-' + rnd.token_hex(16)),
                          'SaltArn': ensure(prefix + 'pseudonym-salt', lambda: rnd.token_hex(32)),
                          # The database master credential, ours instead of
                          # RDS-managed. Shaped EXACTLY like the secret
                          # ManageMasterUserPassword used to produce
                          # ({"username","password"}), so compose_db_url below
                          # is unchanged and reads either one identically.
                          #
                          # token_hex: RDS rejects '/', '"', '@' and space in a
                          # master password, and any of them would also have to
                          # survive URL-encoding into the connection string.
                          # Hex dodges both constraints by construction.
                          #
                          # `ensure` is create-if-missing and NEVER overwrites,
                          # which is what makes this stable: a stack update
                          # re-runs generate and must not change the password
                          # out from under the running database.
                          'DbMasterArn': ensure(
                              prefix + 'db-master',
                              lambda: json.dumps({'username': 'postgres',
                                                  'password': rnd.token_hex(32)})),
                      }
                      return send(event, context, 'SUCCESS', data, prefix + 'generate')
                  if action == 'composeDbUrl':
                      return send(event, context, 'SUCCESS',
                          {'DbUrlArn': put(prefix + 'db-url', compose_db_url(props))},
                          prefix + 'composeDbUrl')
                  return send(event, context, 'FAILED', {'Error': 'unknown action'}, prefix + action)
              except Exception as e:
                  print('SecretFn FAILED:', traceback.format_exc())
                  return send(event, context, 'FAILED', {'Error': str(e)}, event.get('PhysicalResourceId'))

  AppSecrets:
    Type: Custom::AnyraySecrets
    Properties:
      ServiceToken: !GetAtt SecretFn.Arn
      Action: generate
      Prefix: !Sub '${AWS::StackName}/anyray/'
      Names: [content-key, optimizer-token, admin-token, pseudonym-salt, db-master]

  DbUrlSecret:
    Type: Custom::AnyrayDbUrl
    Properties:
      ServiceToken: !GetAtt SecretFn.Arn
      Action: composeDbUrl
      Prefix: !Sub '${AWS::StackName}/anyray/'
      Names: [db-url]
      MasterSecretArn: !GetAtt AppSecrets.DbMasterArn
      DbHost: !GetAtt Db.Endpoint.Address
      DbName: postgres
      # Bumped to re-run the composer on an existing stack. CloudFormation
      # invokes a custom resource on UPDATE only when its properties actually
      # change, so on an unchanged block the upgrade that carries this fix
      # would NOT recompose db-url — a stack whose master has already rotated
      # would stay broken until the schedule below caught it up.
      #
      # 2 (static master): MasterSecretArn above moves from the RDS-managed
      # secret to <stack>/anyray/db-master, which is itself a property change
      # and would re-run this anyway. Bumped regardless — the recompose is what
      # points db-url at the new credential, and it is not worth depending on
      # a subtlety of CFN's diff for the step that decides whether the whole
      # stack can reach its database after the update.
      RefreshVersion: 2

  # ---- db-url convergence (belt and braces) ----------------------------------
  # The composer above is a one-shot: it captures the master password at stack
  # create/update. Since the master password is now OURS and static, this
  # should never have anything to do on a current stack — it is a no-op write
  # when nothing changed.
  #
  # Kept because two populations still drift. A stack created before the static
  # password (its RDS-managed master rotates on AWS's schedule, and its db-url
  # does not follow) heals on the first tick after it takes this template. And
  # an operator who rotates db-master by hand gets convergence without having
  # to know that db-url is derived from it. Deleting this would quietly make
  # both of those permanent breakages again.
  DbUrlRefreshRule:
    Type: AWS::Events::Rule
    Properties:
      Description: !Sub 'Recompose ${AWS::StackName} db-url if the master credential ever drifts.'
      ScheduleExpression: rate(1 hour)
      State: ENABLED
      Targets:
        - Id: secret-fn
          Arn: !GetAtt SecretFn.Arn
          # No RequestType: this is the non-CloudFormation contract in the
          # handler. Same property shape as DbUrlSecret above, deliberately —
          # both paths compose the URL through compose_db_url().
          Input: !Sub |
            {"ResourceProperties": {
              "Action": "composeDbUrl",
              "Prefix": "${AWS::StackName}/anyray/",
              "MasterSecretArn": "${AppSecrets.DbMasterArn}",
              "DbHost": "${Db.Endpoint.Address}",
              "DbName": "postgres"
            }}

  DbUrlRefreshPermission:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt SecretFn.Arn
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com
      SourceArn: !GetAtt DbUrlRefreshRule.Arn

  # The deployment token arrives as a NoEcho parameter; hold it in Secrets
  # Manager and inject it via the task Secrets list so it is not readable by
  # anyone with ecs:DescribeTaskDefinition (the app secrets already flow this
  # way). No explicit Name: a CFN-generated name avoids the scheduled-deletion
  # collision on stack delete + re-create.
  DeploymentTokenSecret:
    Type: AWS::SecretsManager::Secret
    Properties:
      Description: Anyray Cloud deployment token (adt_...) for usage metering.
      SecretString: !Ref DeploymentToken
      Tags: [{ Key: Name, Value: anyray-deployment-token }]

  # ---- Networking / security groups -----------------------------------------
  AlbSg:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Anyray ALB - console (:3000) and gateway (:8787) from a scoped CIDR.
      VpcId: !GetAtt Net.VpcId
      # Ingress is attached below as standalone SecurityGroupIngress resources,
      # fanned out over every value in the AllowedCidr list (Fn::ForEach).
      Tags: [{ Key: Name, Value: anyray-alb }]

  # One ingress rule per (port, CIDR): each loop opens a port to every value in
  # the AllowedCidr list, so a comma-separated AllowedCidr scopes all of them.
  # Fn::ForEach yields resources keyed by the &{Cidr} hash (a valid logical id).
  'Fn::ForEach::AlbGatewayIngress':
    - Cidr
    - !Ref AllowedCidr
    - 'AlbGatewayIngress&{Cidr}':
        Type: AWS::EC2::SecurityGroupIngress
        Properties:
          GroupId: !Ref AlbSg
          IpProtocol: tcp
          FromPort: 8787
          ToPort: 8787
          CidrIp: !Ref Cidr
          Description: Gateway API
  'Fn::ForEach::AlbConsoleIngress':
    - Cidr
    - !Ref AllowedCidr
    - 'AlbConsoleIngress&{Cidr}':
        Type: AWS::EC2::SecurityGroupIngress
        Properties:
          GroupId: !Ref AlbSg
          IpProtocol: tcp
          FromPort: 3000
          ToPort: 3000
          CidrIp: !Ref Cidr
          Description: Console
  'Fn::ForEach::AlbHttpsIngress':
    - Cidr
    - !Ref AllowedCidr
    - 'AlbHttpsIngress&{Cidr}':
        Type: AWS::EC2::SecurityGroupIngress
        Properties:
          GroupId: !Ref AlbSg
          IpProtocol: tcp
          FromPort: 443
          ToPort: 443
          CidrIp: !Ref Cidr
          Description: HTTPS (when CertificateArn set)

  TasksSg:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Anyray Fargate tasks.
      VpcId: !GetAtt Net.VpcId
      Tags: [{ Key: Name, Value: anyray-tasks }]

  TasksFromAlbGateway:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref TasksSg
      IpProtocol: tcp
      FromPort: 8787
      ToPort: 8787
      SourceSecurityGroupId: !Ref AlbSg
      Description: Gateway target from ALB

  TasksFromAlbProxy:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref TasksSg
      IpProtocol: tcp
      FromPort: 80
      ToPort: 80
      SourceSecurityGroupId: !Ref AlbSg
      Description: Console proxy target from ALB

  TasksSelfGateway:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref TasksSg
      IpProtocol: tcp
      FromPort: 8787
      ToPort: 8787
      SourceSecurityGroupId: !Ref TasksSg
      Description: Cloud Map discovery - proxy/optimizer to gateway

  TasksSelfOptimizer:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref TasksSg
      IpProtocol: tcp
      FromPort: 8088
      ToPort: 8088
      SourceSecurityGroupId: !Ref TasksSg
      Description: Cloud Map discovery - gateway/proxy to optimizer

  TasksFromAlbEndpointControl:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref TasksSg
      IpProtocol: tcp
      FromPort: 8090
      ToPort: 8090
      SourceSecurityGroupId: !Ref AlbSg
      Description: Endpoint-control agent-plane target from ALB

  TasksSelfEndpointControl:
    Type: AWS::EC2::SecurityGroupIngress
    Properties:
      GroupId: !Ref TasksSg
      IpProtocol: tcp
      FromPort: 8090
      ToPort: 8090
      SourceSecurityGroupId: !Ref TasksSg
      Description: Cloud Map discovery - gateway to endpoint-control

  RdsSg:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Anyray RDS - Postgres from the Fargate tasks only.
      VpcId: !GetAtt Net.VpcId
      SecurityGroupIngress:
        - { IpProtocol: tcp, FromPort: 5432, ToPort: 5432, SourceSecurityGroupId: !Ref TasksSg, Description: Postgres from tasks }
      Tags: [{ Key: Name, Value: anyray-rds }]

  EfsSg:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Anyray EFS - NFS from the Fargate tasks only.
      VpcId: !GetAtt Net.VpcId
      SecurityGroupIngress:
        - { IpProtocol: tcp, FromPort: 2049, ToPort: 2049, SourceSecurityGroupId: !Ref TasksSg, Description: NFS from tasks }
      Tags: [{ Key: Name, Value: anyray-efs }]

  # ---- RDS PostgreSQL ---------------------------------------------------------
  DbSubnetGroup:
    Type: AWS::RDS::DBSubnetGroup
    Properties:
      DBSubnetGroupDescription: Anyray RDS subnet group.
      SubnetIds: [!GetAtt Net.SubnetA, !GetAtt Net.SubnetB]

  Db:
    Type: AWS::RDS::DBInstance
    DeletionPolicy: Snapshot
    UpdateReplacePolicy: Snapshot
    Properties:
      Engine: postgres
      EngineVersion: '17.5'
      DBInstanceClass: !Ref DbInstanceClass
      AllocatedStorage: !Ref DbAllocatedStorage
      MaxAllocatedStorage: !Ref DbMaxAllocatedStorage
      StorageType: gp3
      StorageEncrypted: true
      MasterUsername: postgres
      # NOT ManageMasterUserPassword. RDS-managed rotation sounds strictly
      # better and was strictly worse here: nothing in this stack follows the
      # rotation, so every rotation silently invalidated the stored connection
      # URL. Running tasks survive on their open pools, so the break stays
      # invisible until the next fresh connection -- a roll, a scale event, the
      # auto-updater -- and then presents as a crash-loop far from its cause.
      # One customer lost 14 days of usage data to exactly that (SQLSTATE
      # 28P01), and no amount of following the rotation removes the race; only
      # not rotating does.
      #
      # The password is minted once by SecretFn's `generate` (create-if-missing,
      # never overwritten) and lives in <stack>/anyray/db-master with the same
      # {"username","password"} shape the RDS-managed secret had, so
      # compose_db_url reads either identically. Rotating it is a deliberate
      # operator act: change the secret, then update the stack.
      MasterUserPassword: !Sub
        - '{{resolve:secretsmanager:${Arn}:SecretString:password}}'
        - Arn: !GetAtt AppSecrets.DbMasterArn
      DBSubnetGroupName: !Ref DbSubnetGroup
      VPCSecurityGroups: [!Ref RdsSg]
      PubliclyAccessible: false
      MultiAZ: !If [Ha, true, false]
      BackupRetentionPeriod: 7
      DeletionProtection: !If [RdsDeletionProtection, true, false]
      Tags: [{ Key: Name, Value: anyray }]

  # ---- EFS for gateway + optimizer /data -------------------------------------
  Efs:
    Type: AWS::EFS::FileSystem
    Properties:
      Encrypted: true
      FileSystemTags: [{ Key: Name, Value: anyray }]

  EfsMountA:
    Type: AWS::EFS::MountTarget
    Properties:
      FileSystemId: !Ref Efs
      SubnetId: !GetAtt Net.SubnetA
      SecurityGroups: [!Ref EfsSg]

  EfsMountB:
    Type: AWS::EFS::MountTarget
    Properties:
      FileSystemId: !Ref Efs
      SubnetId: !GetAtt Net.SubnetB
      SecurityGroups: [!Ref EfsSg]

  GatewayAp:
    Type: AWS::EFS::AccessPoint
    Properties:
      FileSystemId: !Ref Efs
      RootDirectory:
        Path: /gateway
        CreationInfo: { OwnerUid: '0', OwnerGid: '0', Permissions: '0777' }

  OptimizerAp:
    Type: AWS::EFS::AccessPoint
    Properties:
      FileSystemId: !Ref Efs
      RootDirectory:
        Path: /optimizer
        CreationInfo: { OwnerUid: '0', OwnerGid: '0', Permissions: '0777' }

  # ---- ECS cluster + IAM ------------------------------------------------------
  Cluster:
    Type: AWS::ECS::Cluster
    Properties:
      ClusterName: !Sub '${AWS::StackName}-anyray'

  Namespace:
    Type: AWS::ServiceDiscovery::PrivateDnsNamespace
    Properties:
      # Stack-scoped: one VPC can't hold two hosted zones with the same domain.
      Name: !Sub '${AWS::StackName}.anyray.internal'
      Vpc: !GetAtt Net.VpcId

  # Cloud Map service discovery with REAL Route 53 A records — not ECS Service
  # Connect. The console proxy is nginx resolving its gateway upstream at
  # request time via the `resolver` directive, which queries DNS directly and
  # never reads /etc/hosts; Service Connect publishes its aliases ONLY into
  # /etc/hosts (the Cloud Map namespace is registered API-only, no DNS
  # records), so under Service Connect every console request failed its
  # /__auth subrequest and surfaced as a bare nginx 500. Classic service
  # discovery creates records every consumer resolves — nginx (runtime DNS)
  # and the gateway's Node runtime (getaddrinfo) alike.
  #
  # The `-dns` suffix is load-bearing for UPDATES: stacks deployed before this
  # fix have Service Connect-managed Cloud Map services already named
  # `gateway`/`optimizer` in this namespace, and those are only deleted after
  # the old ECS services wind down — long after CloudFormation creates these
  # resources. Reusing the bare names makes a one-step stack update fail with
  # "Service already exists" and roll back.
  GatewayDiscovery:
    Type: AWS::ServiceDiscovery::Service
    Properties:
      Name: gateway-dns
      NamespaceId: !GetAtt Namespace.Id
      DnsConfig:
        DnsRecords: [{ Type: A, TTL: 10 }]
        RoutingPolicy: MULTIVALUE
      HealthCheckCustomConfig: { FailureThreshold: 1 }

  OptimizerDiscovery:
    Type: AWS::ServiceDiscovery::Service
    Properties:
      Name: optimizer-dns
      NamespaceId: !GetAtt Namespace.Id
      DnsConfig:
        DnsRecords: [{ Type: A, TTL: 10 }]
        RoutingPolicy: MULTIVALUE
      HealthCheckCustomConfig: { FailureThreshold: 1 }

  EndpointControlDiscovery:
    Type: AWS::ServiceDiscovery::Service
    Properties:
      Name: endpoint-control-dns
      NamespaceId: !GetAtt Namespace.Id
      DnsConfig:
        DnsRecords: [{ Type: A, TTL: 10 }]
        RoutingPolicy: MULTIVALUE
      HealthCheckCustomConfig: { FailureThreshold: 1 }

  ExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal: { Service: ecs-tasks.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy
      Policies:
        - PolicyName: anyray-read-secrets
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action: secretsmanager:GetSecretValue
                Resource:
                  - !Sub 'arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:${AWS::StackName}/anyray/*'
                  - !Ref DeploymentTokenSecret

  TaskRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal: { Service: ecs-tasks.amazonaws.com }
            Action: sts:AssumeRole
      Policies:
        - PolicyName: anyray-efs
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - elasticfilesystem:ClientMount
                  - elasticfilesystem:ClientWrite
                Resource: !GetAtt Efs.Arn
        # Bedrock served with this role instead of a stored access key. Attached
        # to the TASK role: the execution role only pulls images and reads
        # startup secrets, and is not what application code signs with.
        - !If
          - Bedrock
          - PolicyName: anyray-bedrock
            PolicyDocument:
              Version: '2012-10-17'
              Statement:
                # Invoke is scoped to models and inference profiles, and spans
                # regions on purpose: a cross-region inference profile fans out
                # to the foundation model in whichever region serves the call,
                # so pinning this to the stack's own region breaks exactly the
                # profiles Claude 4 and newer require.
                - Effect: Allow
                  Action:
                    - bedrock:InvokeModel
                    - bedrock:InvokeModelWithResponseStream
                  Resource:
                    - !Sub 'arn:${AWS::Partition}:bedrock:*::foundation-model/*'
                    - !Sub 'arn:${AWS::Partition}:bedrock:*:${AWS::AccountId}:inference-profile/*'
                    - !Sub 'arn:${AWS::Partition}:bedrock:*:${AWS::AccountId}:application-inference-profile/*'
                # Read-only discovery: turns a Claude model id into the id
                # Bedrock actually serves here. These two actions do not accept
                # a resource scope, so `*` is the only valid form.
                - Effect: Allow
                  Action:
                    - bedrock:ListFoundationModels
                    - bedrock:ListInferenceProfiles
                  Resource: '*'
          - !Ref 'AWS::NoValue'

  # ---- Soft-update applier ----------------------------------------------------
  # The gateway owns the DECISION: it polls the release manifest, evaluates it
  # against this deployment (updatePreflight.classifyUpdate) and only triggers
  # for a `soft` release — image swap only, preflight green, no breaking notes.
  # Schema migrations self-apply on boot, so they never make a release hard.
  # Hard releases are never applied unattended and stay on the notify path.
  #
  # Compose bundles Watchtower to execute that decision. ECS has no equivalent,
  # so this is it: same contract Watchtower speaks (POST + bearer token), and it
  # rolls every app service together so they can never land on different
  # versions. The services already run a deployment circuit breaker with
  # rollback, so a release that fails its health checks reverts itself.
  UpdaterRole:
    Type: AWS::IAM::Role
    Condition: AutoUpdate
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal: { Service: lambda.amazonaws.com }
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
      Policies:
        - PolicyName: roll-services
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              # Scoped to this stack's cluster. Deliberately NOT the individual
              # service ARNs: the gateway task definition consumes this
              # function's URL, so depending on the services would make the
              # stack circular.
              - Effect: Allow
                Action: [ecs:ListServices, ecs:DescribeServices, ecs:UpdateService]
                Resource: '*'
                Condition:
                  ArnEquals:
                    ecs:cluster: !GetAtt Cluster.Arn
              # Task-definition APIs are not resource-scopable.
              - Effect: Allow
                Action: [ecs:DescribeTaskDefinition, ecs:RegisterTaskDefinition]
                Resource: '*'
              - Effect: Allow
                Action: iam:PassRole
                Resource: [!GetAtt ExecutionRole.Arn, !GetAtt TaskRole.Arn]
              # Verifies the caller's bearer token against the admin token.
              - Effect: Allow
                Action: secretsmanager:GetSecretValue
                Resource: !GetAtt AppSecrets.AdminTokenArn
              # Refresh db-url immediately before rolling (see REFRESH_PAYLOAD
              # in the updater below). The compose logic stays in SecretFn --
              # one function, one definition of the URL -- so this is an
              # invoke, never a second copy of compose_db_url().
              - Effect: Allow
                Action: lambda:InvokeFunction
                Resource: !GetAtt SecretFn.Arn

  UpdaterFn:
    Type: AWS::Lambda::Function
    Condition: AutoUpdate
    Properties:
      Runtime: python3.12
      Handler: index.handler
      Timeout: 120
      Role: !GetAtt UpdaterRole.Arn
      Environment:
        Variables:
          CLUSTER: !Ref Cluster
          FAMILY_PREFIX: !Sub '${AWS::StackName}-'
          TOKEN_SECRET_ARN: !GetAtt AppSecrets.AdminTokenArn
          INDEX_URL: https://charts.anyray.ai/index.yaml
          # Recompose db-url from the rotated master immediately before the
          # roll. SecretFn owns the compose; this is the same bare-properties
          # contract DbUrlRefreshRule uses (no RequestType, no ResponseURL).
          SECRET_FN_ARN: !GetAtt SecretFn.Arn
          REFRESH_PAYLOAD: !Sub |
            {"ResourceProperties": {
              "Action": "composeDbUrl",
              "Prefix": "${AWS::StackName}/anyray/",
              "MasterSecretArn": "${AppSecrets.DbMasterArn}",
              "DbHost": "${Db.Endpoint.Address}",
              "DbName": "postgres"
            }}
      Code:
        ZipFile: |
          import base64, hmac, json, os, re, urllib.request, traceback, boto3
          ecs = boto3.client('ecs')
          sm = boto3.client('secretsmanager')
          lam = boto3.client('lambda')
          # The `v` is optional on the way IN and always present on the way OUT
          # (release_tag). The control plane serves versions bare -- it strips the
          # prefix -- while published images are tagged `vX.Y.Z`, so a gateway
          # asking for "1.10.246" means the image `:v1.10.246`. Requiring the `v`
          # here would 500 every unattended update the gateway ever sends.
          SEMVER = re.compile(r'^v?(\d+)\.(\d+)\.(\d+)$')
          # Mirrors MAX_PREFLIGHT_TARGET_CHARS gateway-side: the regex alone would
          # admit an arbitrarily long digit run as a "version".
          MAX_TARGET_CHARS = 20
          FAMILIES = ('gateway', 'optimizer', 'proxy', 'endpoint-control')

          class BadRequest(Exception):
              """Caller's input is malformed -- a 400, never a 500."""

          def reply(code, body):
              return {'statusCode': code,
                      'headers': {'content-type': 'application/json'},
                      'body': json.dumps(body)}

          def parse_version(tag):
              """(major, minor, patch) for an immutable release tag, else None."""
              m = SEMVER.fullmatch((tag or '').strip())
              return tuple(int(x) for x in m.groups()) if m else None

          def release_tag(parsed):
              """Canonical published-image tag for a parsed version."""
              return 'v%d.%d.%d' % parsed

          def authorised(event):
              hdrs = {k.lower(): v for k, v in (event.get('headers') or {}).items()}
              parts = (hdrs.get('authorization') or '').split(None, 1)
              # Check the header's SHAPE before spending a GetSecretValue. This
              # URL is public by design (AuthType NONE), so without this every
              # unauthenticated request bills a Secrets Manager call. Matching
              # the scheme properly also closes a slice that assumed the prefix:
              # the old code took everything past 7 characters, so any 7-byte
              # preamble was accepted in place of "Bearer ".
              if len(parts) != 2 or parts[0].lower() != 'bearer':
                  return False
              presented = parts[1].strip()
              if not presented:
                  return False
              expected = sm.get_secret_value(
                  SecretId=os.environ['TOKEN_SECRET_ARN'])['SecretString']
              return hmac.compare_digest(presented, expected)

          def latest_release():
              with urllib.request.urlopen(os.environ['INDEX_URL'], timeout=20) as r:
                  body = r.read().decode('utf-8', 'replace')
              tags = set(re.findall(r'appVersion:\s*(v\d+\.\d+\.\d+)\s*$', body, re.M))
              if not tags:
                  raise Exception('no appVersion entries in the release index')
              return max(tags, key=lambda t: tuple(int(x) for x in SEMVER.match(t).groups()))

          def requested_target(event):
              """The tag the CALLER asked for, if it named one.

              The gateway decides its own target: it evaluates the release
              manifest against this deployment and only asks for a version it
              classified as a soft update. Honour that instead of resolving
              'latest' here, because the two sources disagree -- the chart index
              this Lambda reads lags the manifest the gateway reads. Resolving
              our own then finds every service already current, answers 200, and
              the gateway records a successful trigger that changed nothing,
              suppressing its retry for six hours.

              Validated against the same immutable-tag regex either way, so a
              caller can only ever name a release tag, never an arbitrary image.
              Absent/blank body keeps the old behaviour (console one-click).
              """
              raw = event.get('body') or ''
              if event.get('isBase64Encoded'):
                  raw = base64.b64decode(raw).decode('utf-8', 'replace')
              if not raw.strip():
                  return None
              try:
                  parsed = json.loads(raw)
              except Exception:
                  raise BadRequest('request body was not valid JSON')
              if not isinstance(parsed, dict):
                  raise BadRequest('request body must be a JSON object')
              target = parsed.get('target')
              if target is None:
                  return None
              if not isinstance(target, str) or len(target) > MAX_TARGET_CHARS:
                  raise BadRequest('target must be an immutable release tag like v1.10.245')
              version = parse_version(target)
              if not version:
                  raise BadRequest('target must be an immutable release tag like v1.10.245')
              # Re-emit from the parsed groups rather than echoing the caller's
              # string, so nothing but digits can reach an image reference.
              return release_tag(version)

          def refresh_db_url():
              """Recompose db-url from the RDS master secret, before any roll.

              THE POINT: a rolled service starts FRESH tasks, and a fresh task
              is the first thing in a while to actually use the credential.
              RDS rotates the master on its own schedule
              (ManageMasterUserPassword) while running tasks keep working off
              their already-open pools, so the stale URL stays invisible until
              exactly this moment -- and then presents as a crash-loop, which
              trips the ECS deployment circuit breaker and can wedge the stack
              in UPDATE_ROLLBACK_FAILED. An unattended nightly update is
              enough to fire it with no human involved.

              DbUrlRefreshRule already runs hourly; this closes the gap it
              cannot -- a rotation at T followed by a roll at T+10min. Rolling
              is the only moment that MUST have a current credential, so it is
              worth spending one invoke to be sure.

              Delegates to SecretFn rather than composing here: the refresh
              decides whether to write by comparing the composed URL against
              the stored one, so a one-byte drift between two copies of that
              logic would rewrite the secret forever. One function, one
              definition.
              """
              arn = os.environ.get('SECRET_FN_ARN')
              payload = os.environ.get('REFRESH_PAYLOAD')
              if not arn or not payload:
                  # A stack updated to this template always has both. Absent
                  # means an older stack; roll as before rather than refuse.
                  print('db-url refresh not configured; rolling without it')
                  return
              res = lam.invoke(FunctionName=arn, InvocationType='RequestResponse',
                               Payload=payload.encode('utf-8'))
              if res.get('FunctionError'):
                  # Do NOT roll. Registering fresh tasks against a credential we
                  # could not confirm is how the crash-loop above starts, and a
                  # skipped update is recoverable where a wedged stack is not.
                  # The gateway retries on its own schedule.
                  raise Exception('db-url refresh failed: %s'
                                  % res['FunctionError'])
              body = json.loads(res['Payload'].read().decode('utf-8') or '{}')
              # Never log the URL: it carries the master password.
              print('db-url refresh:', json.dumps(body))

          def services_for(cluster, prefix):
              """This stack's app services, found by task-definition family."""
              out, token = [], None
              while True:
                  page = ecs.list_services(cluster=cluster, maxResults=100,
                                           **({'nextToken': token} if token else {}))
                  arns = page.get('serviceArns') or []
                  if arns:
                      for svc in ecs.describe_services(
                              cluster=cluster, services=arns)['services']:
                          family = svc['taskDefinition'].split('/')[-1].split(':')[0]
                          if family in [prefix + f for f in FAMILIES]:
                              out.append((svc['serviceName'], family))
                  token = page.get('nextToken')
                  if not token:
                      break
              return out

          def handler(event, context):
              try:
                  if not authorised(event):
                      return reply(403, {'status': 'failure',
                                         'message': 'bad or missing bearer token'})
                  asked = requested_target(event)
                  tag = asked or latest_release()
                  wanted = parse_version(tag)
                  cluster = os.environ['CLUSTER']
                  # Read-only preflight over EVERY service before ANY mutation.
                  # Validating each service inside the update loop meant a
                  # mixed-version stack (gateway v1, proxy v3, target v2) moved
                  # the gateway FIRST and then rejected on the proxy: the caller
                  # got a 400 for a deployment that had in fact partially
                  # updated, and the services it exists to keep on one version
                  # were left on two. Any rejection now happens with zero
                  # RegisterTaskDefinition / UpdateService calls behind it. The
                  # preflight walks whatever services_for() returns, so it needs
                  # no change when FAMILIES grows.
                  plan, current = [], []
                  for service, family in services_for(cluster, os.environ['FAMILY_PREFIX']):
                      td = ecs.describe_task_definition(taskDefinition=family)['taskDefinition']
                      if all(c['image'].rsplit(':', 1)[-1] == tag
                             for c in td['containerDefinitions']):
                          current.append(service)
                          continue
                      # Forward only. This endpoint exists to APPLY updates; a
                      # rollback is a deliberate operator act and goes through a
                      # CloudFormation stack update, where it is recorded. Before
                      # the caller could name a version, the tag always came from
                      # the release index and could only move forward; honouring a
                      # request without this check would hand anyone holding the
                      # token a one-call downgrade to a release with known holes.
                      # The gateway enforces the same rule (updatePreflight), but
                      # that is the caller's check, not ours.
                      running = [parse_version(c['image'].rsplit(':', 1)[-1])
                                 for c in td['containerDefinitions']]
                      if any(cur and wanted < cur for cur in running):
                          raise BadRequest(
                              'refusing to move %s backwards; roll back through '
                              'a CloudFormation stack update instead' % service)
                      plan.append((service, td))
                  # Only a fully valid set reaches this point; mutate it.
                  #
                  # Refresh the database credential FIRST, and only when there
                  # is something to roll. Placed after validation so a rejected
                  # or already-current request never touches Secrets Manager,
                  # and before the first register_task_definition so every task
                  # this roll starts reads a credential composed seconds ago.
                  if plan:
                      refresh_db_url()
                  updated = []
                  for service, td in plan:
                      for k in ('taskDefinitionArn', 'revision', 'status',
                                'requiresAttributes', 'compatibilities',
                                'registeredAt', 'registeredBy', 'deregisteredAt'):
                          td.pop(k, None)
                      for c in td['containerDefinitions']:
                          c['image'] = c['image'].rsplit(':', 1)[0] + ':' + tag
                      arn = ecs.register_task_definition(**td)['taskDefinition']['taskDefinitionArn']
                      ecs.update_service(cluster=cluster, service=service, taskDefinition=arn)
                      updated.append(service)
                  # 'target' names where the tag came from: a caller-requested
                  # version, or one this Lambda resolved from the chart index.
                  # A roll that keeps reporting alreadyCurrent on 'index' is the
                  # index lagging, not the deployment being up to date.
                  result = {'status': 'ok', 'tag': tag,
                            'target': 'requested' if asked else 'index',
                            'updated': updated, 'alreadyCurrent': current}
                  print(json.dumps(result))
                  return reply(200, result)
              except BadRequest as e:
                  # 400, not 500: the caller sent something this endpoint will
                  # never accept, and retrying verbatim cannot help. A 500 here
                  # reads to the gateway as "unreachable" and sends whoever is
                  # debugging after the network instead of the contract.
                  print('updater REJECTED:', str(e))
                  return reply(400, {'status': 'failure', 'message': str(e)})
              except Exception:
                  # The traceback goes to CloudWatch; the CALLER gets nothing
                  # but a generic failure. This handler also covers exceptions
                  # raised by authorised() itself, so on an endpoint that is
                  # public by design, echoing str(e) hands an UNAUTHENTICATED
                  # caller internal detail: a missing TOKEN_SECRET_ARN raises a
                  # KeyError naming it, and a Secrets Manager denial quotes the
                  # secret's full ARN. Constant-string BadRequest messages above
                  # stay verbatim; they are ours and say nothing about the
                  # deployment.
                  print('updater FAILED:', traceback.format_exc())
                  return reply(500, {'status': 'failure',
                                     'message': 'internal error; see the updater '
                                                'function log group'})

  # AuthType NONE because the caller is the gateway container, which has no AWS
  # credentials to sign with — the bearer check above is the gate, exactly as
  # Watchtower's HTTP API works on compose.
  UpdaterUrl:
    Type: AWS::Lambda::Url
    Condition: AutoUpdate
    Properties:
      TargetFunctionArn: !GetAtt UpdaterFn.Arn
      AuthType: NONE

  UpdaterUrlPermission:
    Type: AWS::Lambda::Permission
    Condition: AutoUpdate
    Properties:
      FunctionName: !GetAtt UpdaterFn.Arn
      Action: lambda:InvokeFunctionUrl
      Principal: '*'
      FunctionUrlAuthType: NONE

  # BOTH grants are required. Lambda's URL front door now authorises
  # `lambda:InvokeFunction` as well as `lambda:InvokeFunctionUrl`, so with only
  # the statement above EVERY caller gets a service-layer 403 ("Forbidden",
  # from the front door) before the function's own bearer check ever runs — the
  # gateway's auto-update trigger then fails on every attempt, and logs it
  # misleadingly as a token mismatch. Verified against a live Function URL:
  # InvokeFunctionUrl alone → 403, both → 200.
  #
  # This one carries no FunctionUrlAuthType condition — Lambda rejects the
  # property outright on any action other than InvokeFunctionUrl ("only
  # supported for lambda:InvokeFunctionUrl"), which fails the stack. It widens
  # nothing in practice: the URL above is already AuthType NONE, so the
  # endpoint is public by design and the handler's bearer check against the
  # admin token is the actual gate, on both paths.
  UpdaterInvokePermission:
    Type: AWS::Lambda::Permission
    Condition: AutoUpdate
    Properties:
      FunctionName: !GetAtt UpdaterFn.Arn
      Action: lambda:InvokeFunction
      Principal: '*'

  GatewayLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/anyray/${AWS::StackName}/gateway'
      RetentionInDays: 30
  OptimizerLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/anyray/${AWS::StackName}/optimizer'
      RetentionInDays: 30
  ProxyLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/anyray/${AWS::StackName}/proxy'
      RetentionInDays: 30
  EndpointControlLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/anyray/${AWS::StackName}/endpoint-control'
      RetentionInDays: 30

  # ---- Load balancer ----------------------------------------------------------
  Alb:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Type: application
      # internet-facing (default) or internal, per LoadBalancerScheme. "internal"
      # gives the load balancer private addresses only, so the whole deployment is
      # reachable only from inside the VPC or a network joined to it. The ECS tasks
      # are unaffected: they run with their own public IP for egress, so the gateway
      # still reaches model providers and Anyray Cloud with no NAT gateway. The
      # subnets are shared by both schemes, which AWS allows.
      Scheme: !Ref LoadBalancerScheme
      Subnets: [!GetAtt Net.SubnetA, !GetAtt Net.SubnetB]
      SecurityGroups: [!Ref AlbSg]
      LoadBalancerAttributes:
        # How long the ALB holds a connection with NO bytes moving on it before
        # closing. AWS defaults this to 60s, which is wrong for this workload in
        # two separate ways, and left unset it silently undid both of the
        # gateway's own timeout fixes on every AWS install:
        #
        #  * IN-FLIGHT. A completion is silent between the request and its first
        #    token. On a large prompt with extended thinking that gap runs well
        #    past a minute, so the ALB closed the connection while the gateway
        #    was still waiting on the provider — a bare transport close the
        #    developer's tool reports as a failed request.
        #  * IDLE POOLED SOCKETS. Coding tools keep connections to the gateway
        #    pooled across turns, and a real session's think-gap between turns
        #    routinely exceeds a minute. The gateway holds those sockets for 10
        #    minutes for exactly this reason (DEFAULT_KEEPALIVE_TIMEOUT_MS,
        #    gateway/src/utils/serverTimeouts.ts) — but on AWS the ALB is the
        #    peer the client actually pools against, so its 60s was the binding
        #    limit and the reused socket RST'd (client ECONNRESET) regardless.
        #
        # It must stay BELOW the gateway's keep-alive (600s) rather than above
        # it: whichever side closes an idle socket first should be the ALB, or
        # it forwards a request onto a socket the target has just closed and
        # answers 502. 300s clears both cases above with a wide margin and keeps
        # that ordering intact.
        #
        # CHANGE-BOTH-TOGETHER: lower DEFAULT_KEEPALIVE_TIMEOUT_MS in the
        # monorepo below this value and the 502 race comes back.
        - { Key: idle_timeout.timeout_seconds, Value: '300' }

  GatewayTg:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      VpcId: !GetAtt Net.VpcId
      TargetType: ip
      Port: 8787
      Protocol: HTTP
      HealthCheckPath: /
      Matcher: { HttpCode: 200-399 }
      # Left unset, AWS applies 30s / 5 healthy / 2 unhealthy, and BOTH halves of
      # that hurt here. Detection: a task that dies or wedges keeps receiving
      # traffic for 2 x 30s = a full minute, which is most of the outage a second
      # task exists to prevent. Rotation: a replacement needs 5 x 30s = 150s of
      # passing checks before it may serve, so every rolling deploy holds the old
      # tasks 2.5 minutes longer than it needs to and the surge capacity is paid
      # for twice.
      #
      # `/` is the gateway's readiness route (503 while draining or awaiting the
      # entitlement lease) and costs nothing to serve, so it takes a tight
      # interval. UnhealthyThresholdCount stays at 3 rather than 2: at a 10s
      # interval two consecutive misses is 20s, close enough to a GC pause or a
      # slow moment to pull a healthy task, and pulling a good target is itself
      # the outage. Three misses = 30s, still half the AWS default.
      #
      # This cannot restart anything — ECS container replacement is driven by the
      # task definition's own HealthCheck, not by this. The worst case is a
      # briefly slow gateway leaving rotation and rejoining 20s later.
      HealthCheckIntervalSeconds: 10
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      TargetGroupAttributes:
        # How long the ALB lets in-flight requests finish on a target it is
        # deregistering (every deploy). /v1 streams run for minutes, so the
        # stock-ish 15s used to close them from the LOAD BALANCER side while the
        # task was still draining happily — a bare transport close the gateway
        # never sees and the developer's tool reports as "Connection closed
        # mid-response". Kept at/above the task's StopTimeout so the ALB is never
        # the first to give up.
        - { Key: deregistration_delay.timeout_seconds, Value: '120' }

  ProxyTg:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      VpcId: !GetAtt Net.VpcId
      TargetType: ip
      Port: 80
      Protocol: HTTP
      HealthCheckPath: /anyray-login
      Matcher: { HttpCode: 200-399 }
      # Same reasoning as the gateway target group above: the AWS defaults leave
      # a dead console task in rotation for a minute and hold a replacement out
      # for 150s. /anyray-login is served by nginx itself with no gateway
      # round-trip, so it reports the proxy's own health and is cheap to poll.
      HealthCheckIntervalSeconds: 10
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      TargetGroupAttributes:
        # Console requests are short and, unlike Compose, this proxy carries no
        # /v1 stream on AWS (the gateway has its own listener on 8787), so 15s is
        # the right deregistration delay here rather than the gateway's 120s.
        - { Key: deregistration_delay.timeout_seconds, Value: '15' }

  EndpointControlTg:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      VpcId: !GetAtt Net.VpcId
      TargetType: ip
      Port: 8090
      Protocol: HTTP
      # ALB health = readiness: /readyz checks Postgres and turns 503 while
      # draining, so a deploy pulls a task from rotation before it stops.
      # (/livez, the process-only liveness route, is the CONTAINER health
      # check on the task definition — the service's documented probe split.)
      HealthCheckPath: /readyz
      Matcher: { HttpCode: 200-399 }
      # Same reasoning as the gateway target group above.
      HealthCheckIntervalSeconds: 10
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      TargetGroupAttributes:
        # 35s, not 15s: the service bounds a single request at 30s
        # (server.requestTimeout, endpoint-control/src/index.ts) and its own
        # shutdown drain force-exits at 35s. At 15s the ALB was the first to give
        # up, closing an agent-plane call the container was still serving — the
        # same class of bug the gateway's 120s fixed, one order of magnitude
        # down. Matched to the drain so the container, not the load balancer,
        # decides when a request has run out of time.
        - { Key: deregistration_delay.timeout_seconds, Value: '35' }

  GatewayListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref Alb
      Port: 8787
      Protocol: !If [HasCert, HTTPS, HTTP]
      Certificates: !If [HasCert, [{ CertificateArn: !Ref CertificateArn }], !Ref 'AWS::NoValue']
      DefaultActions:
        - { Type: forward, TargetGroupArn: !Ref GatewayTg }

  # End-point control agent plane (RFC 0014): fleetd on employee laptops calls
  # these FIXED root-anchored paths against the deployment's existing gateway
  # origin (the gateway has no /api/* routes, so nothing collides and no second
  # certificate is needed). ONLY these path families reach the service — its
  # /admin/*, /readyz and /livez are never routed at the listener, mirroring the
  # vendor edge's allowlist posture — and everything unmatched still falls
  # through to the gateway default action.
  #
  # /livez is excluded on purpose even though the vendor ALB forwards it: that
  # ALB is endpoint-control's own, this listener is the GATEWAY's. The gateway
  # serves /livez itself and the docs point operators' liveness checks at it, so
  # a rule here would silently re-aim every external probe on :8787 at the wrong
  # process — 200 while the gateway is down, 503 whenever this target group has
  # no healthy target and the gateway is fine. Target health rides the target
  # group's own /readyz check; nothing external needs /livez. This mirrors the
  # same deliberate omission in infra/aws/modules/endpoint-control/alb.tf.
  EndpointAgentRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref GatewayListener
      Priority: 10
      Conditions:
        - Field: path-pattern
          PathPatternConfig:
            Values: ['/api/v1/osquery/*', '/api/fleet/orbit/*', '/api/v1/evidence/*']
      Actions:
        - { Type: forward, TargetGroupArn: !Ref EndpointControlTg }

  ConsoleListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref Alb
      Port: 3000
      Protocol: !If [HasCert, HTTPS, HTTP]
      Certificates: !If [HasCert, [{ CertificateArn: !Ref CertificateArn }], !Ref 'AWS::NoValue']
      DefaultActions:
        - { Type: forward, TargetGroupArn: !Ref ProxyTg }

  # ---- Task definitions -------------------------------------------------------
  OptimizerTask:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub '${AWS::StackName}-optimizer'
      RequiresCompatibilities: [FARGATE]
      NetworkMode: awsvpc
      # 1 vCPU / 2 GB. The optimizer runs onnxruntime (MiniLM re-rank, OCR)
      # synchronously on the Node event loop; at fractional vCPU a single
      # inference pegs CPU to 100%, blocking the loop long enough to miss the
      # health probe and trigger recycle churn (exit 137). A full vCPU shortens
      # each inference block and keeps the gateway's optimize call landing before
      # it times out. Cpu 1024 requires Memory >= 2048 (Fargate valid combos).
      Cpu: '1024'
      Memory: '2048'
      ExecutionRoleArn: !GetAtt ExecutionRole.Arn
      TaskRoleArn: !GetAtt TaskRole.Arn
      Volumes:
        - Name: data
          EFSVolumeConfiguration:
            FilesystemId: !Ref Efs
            TransitEncryption: ENABLED
            AuthorizationConfig: { AccessPointId: !Ref OptimizerAp, IAM: ENABLED }
      ContainerDefinitions:
        - Name: optimizer
          Image: !Sub 'public.ecr.aws/anyray/optimizer:${ImageTag}'
          Essential: true
          # Seconds ECS waits after SIGTERM before SIGKILL. The optimizer drains
          # in-flight optimize calls for ANYRAY_SHUTDOWN_DRAIN_MS (default
          # 15000) — far less than the gateway's 90s, because an optimize call
          # is bounded by the gateway's own ceiling on it (800ms, 10s for
          # vision) and not by how long a model takes to answer. Stated rather
          # than left to the ECS default of 30s, which happens to be enough
          # today and would silently stop being enough the moment anyone raised
          # that drain. Nothing here is user-visible even when it is cut — the
          # gateway fails open on an unreachable optimizer — but each cut call
          # busts that session's prompt cache, which is money.
          StopTimeout: 30
          PortMappings:
            - { Name: optimizer, ContainerPort: 8088, Protocol: tcp }
          MountPoints:
            - { SourceVolume: data, ContainerPath: /data }
          Environment:
            - { Name: PORT, Value: '8088' }
            - { Name: ANYRAY_DATA_DIR, Value: /data }
            - { Name: ANYRAY_CONTENT_MODE, Value: !Ref ContentMode }
            - { Name: ANYRAY_ALLOW_PLAINTEXT, Value: !Ref AllowPlaintext }
            # Same trace-retention sweep as the gateway — must see the same override.
            - { Name: ANYRAY_SPEND_RETENTION_DAYS, Value: !Ref SpendRetentionDays }
          Secrets:
            - { Name: ANYRAY_OPTIMIZER_TOKEN, ValueFrom: !GetAtt AppSecrets.OptimizerTokenArn }
            - { Name: ANYRAY_ADMIN_TOKEN, ValueFrom: !GetAtt AppSecrets.AdminTokenArn }
            # Durable CCR stash tier (same RDS the gateway uses): stashed
            # originals persist as ciphertext, so retrieval handles survive task
            # restarts and resolve from any replica if DesiredCount is raised.
            - { Name: ANYRAY_SPEND_DB_URL, ValueFrom: !GetAtt DbUrlSecret.DbUrlArn }
            - { Name: ANYRAY_CONTENT_KEY, ValueFrom: !GetAtt AppSecrets.ContentKeyArn }
          HealthCheck:
            Command: [CMD-SHELL, 'node -e "fetch(''http://localhost:8088/health'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"']
            Interval: 10
            Timeout: 5
            Retries: 10
            StartPeriod: 20
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref OptimizerLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: optimizer

  GatewayTask:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub '${AWS::StackName}-gateway'
      RequiresCompatibilities: [FARGATE]
      NetworkMode: awsvpc
      Cpu: '512'
      Memory: '1024'
      ExecutionRoleArn: !GetAtt ExecutionRole.Arn
      TaskRoleArn: !GetAtt TaskRole.Arn
      Volumes:
        - Name: data
          EFSVolumeConfiguration:
            FilesystemId: !Ref Efs
            TransitEncryption: ENABLED
            AuthorizationConfig: { AccessPointId: !Ref GatewayAp, IAM: ENABLED }
      ContainerDefinitions:
        - Name: gateway
          Image: !Sub 'public.ecr.aws/anyray/gateway:${ImageTag}'
          Essential: true
          # Seconds ECS waits after SIGTERM before SIGKILL. Must exceed the
          # gateway's own drain (ANYRAY_SHUTDOWN_DRAIN_MS, default 90000), or
          # every deploy kills the streaming turns it was in the middle of
          # finishing. ECS's own default is 30s — short enough to cut an ordinary
          # coding request — and 120 is the Fargate maximum. A ceiling, not a
          # delay: a task with nothing in flight still stops at once.
          StopTimeout: 120
          PortMappings:
            - { Name: gateway, ContainerPort: 8787, Protocol: tcp }
          MountPoints:
            - { SourceVolume: data, ContainerPath: /data }
          Environment:
            - { Name: ANYRAY_OPTIMIZER_URL, Value: !Sub 'http://optimizer-dns.${AWS::StackName}.anyray.internal:8088' }
            # In-VPC address of the endpoint-control service (RFC 0014).
            # Optional on the gateway: images that predate the end-point lane
            # ignore it, so it is safe ahead of the gateway that reads it.
            - { Name: ANYRAY_ENDPOINT_CONTROL_URL, Value: !Sub 'http://endpoint-control-dns.${AWS::StackName}.anyray.internal:8090' }
            # Gives the gateway's existing soft-update loop an applier on ECS.
            # Unset (EnableAutoUpdate=false) => the loop no-ops and this install
            # stays notify-only, exactly like Kubernetes and Railway.
            - !If
              - AutoUpdate
              - { Name: ANYRAY_UPDATER_URL, Value: !GetAtt UpdaterUrl.FunctionUrl }
              - !Ref 'AWS::NoValue'
            - { Name: ANYRAY_OPTIMIZER_TIMEOUT_MS, Value: !Ref OptimizerTimeoutMs }
            - { Name: ANYRAY_OPTIMIZER_VISION_TIMEOUT_MS, Value: !Ref OptimizerVisionTimeoutMs }
            - { Name: ANYRAY_DEFAULT_MODEL, Value: !Ref DefaultModel }
            - { Name: ANYRAY_ALLOW_PLAINTEXT, Value: !Ref AllowPlaintext }
            - { Name: ANYRAY_DATA_DIR, Value: /data }
            - { Name: ANYRAY_TRUST_PROXY, Value: 'true' }
            - { Name: ANYRAY_HSTS, Value: !Ref Hsts }
            - { Name: ANYRAY_RATE_LIMIT_RPM, Value: !Ref RateLimitRpm }
            - { Name: ANYRAY_RATE_LIMIT_IP_RPM, Value: !Ref RateLimitIpRpm }
            - { Name: ANYRAY_RATE_LIMIT_UNAUTH_RPM, Value: !Ref RateLimitUnauthRpm }
            - { Name: ANYRAY_SPEND_RETENTION_DAYS, Value: !Ref SpendRetentionDays }
            - { Name: ANYRAY_MAX_CONCURRENT_REQUESTS, Value: !Ref MaxConcurrentRequests }
            - { Name: ANYRAY_AUTH_FAIL_LIMIT, Value: !Ref AuthFailLimit }
            - { Name: ANYRAY_MAX_BODY_BYTES, Value: !Ref MaxBodyBytes }
            - { Name: ANYRAY_CUSTOM_HOST_ALLOWLIST, Value: !Ref CustomHostAllowlist }
            - { Name: TRUSTED_CUSTOM_HOSTS, Value: !Ref TrustedCustomHosts }
            - { Name: ANYRAY_UPSTREAM_URL, Value: !Ref UpstreamUrl }
            - { Name: ANYRAY_METERING_ENABLED, Value: 'true' }
            - { Name: ANYRAY_SEAT_EXCLUDE, Value: !Ref SeatExclude }
            - { Name: ANYRAY_METERING_INTERVAL_MS, Value: !Ref MeteringIntervalMs }
            - { Name: ANYRAY_ENTITLEMENT_GRACE_MS, Value: !Ref EntitlementGraceMs }
            - Name: ANYRAY_GATEWAY_PUBLIC_URL
              Value: !If
                - HasGatewayUrl
                - !Ref GatewayPublicUrl
                - !If [HasCert, !Sub 'https://${Alb.DNSName}:8787', !Sub 'http://${Alb.DNSName}:8787']
            - Name: ANYRAY_CONSOLE_PUBLIC_URL
              Value: !If
                - HasConsoleUrl
                - !Ref ConsolePublicUrl
                - !If [HasCert, !Sub 'https://${Alb.DNSName}:3000', !Sub 'http://${Alb.DNSName}:3000']
          Secrets:
            - { Name: ANYRAY_SPEND_DB_URL, ValueFrom: !GetAtt DbUrlSecret.DbUrlArn }
            - { Name: ANYRAY_OBSERVABILITY_DB_URL, ValueFrom: !GetAtt DbUrlSecret.DbUrlArn }
            - { Name: ANYRAY_CONTENT_KEY, ValueFrom: !GetAtt AppSecrets.ContentKeyArn }
            - { Name: ANYRAY_ADMIN_TOKEN, ValueFrom: !GetAtt AppSecrets.AdminTokenArn }
            - { Name: ANYRAY_OPTIMIZER_TOKEN, ValueFrom: !GetAtt AppSecrets.OptimizerTokenArn }
            - { Name: ANYRAY_PSEUDONYM_SALT, ValueFrom: !GetAtt AppSecrets.SaltArn }
            - { Name: ANYRAY_DEPLOYMENT_TOKEN, ValueFrom: !Ref DeploymentTokenSecret }
          HealthCheck:
            Command: [CMD-SHELL, 'node -e "fetch(''http://localhost:8787/'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"']
            Interval: 10
            Timeout: 5
            Retries: 10
            StartPeriod: 60
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref GatewayLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: gateway

  # End-point control (RFC 0014) — the client-operated device-evidence service.
  # Stateless (no volume): everything durable lives in the same RDS database
  # the gateway's spend store uses (boot-applied idempotent schema).
  EndpointControlTask:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub '${AWS::StackName}-endpoint-control'
      RequiresCompatibilities: [FARGATE]
      NetworkMode: awsvpc
      Cpu: '256'
      Memory: '512'
      ExecutionRoleArn: !GetAtt ExecutionRole.Arn
      TaskRoleArn: !GetAtt TaskRole.Arn
      ContainerDefinitions:
        - Name: endpoint-control
          Image: !Sub 'public.ecr.aws/anyray/endpoint-control:${ImageTag}'
          Essential: true
          # The service drains on SIGTERM and force-exits that drain at 35s
          # (endpoint-control/src/index.ts), which is LONGER than the ECS
          # default of 30s — so the default had the platform SIGKILL the
          # container five seconds before its own shutdown path would have
          # finished, cutting agent-plane calls and skipping the store close.
          # 45 leaves the container, not the platform, deciding when it is done.
          StopTimeout: 45
          PortMappings:
            - { Name: endpoint-control, ContainerPort: 8090, Protocol: tcp }
          Environment:
            # The public origin fleetd on employee laptops is pointed at: the
            # SAME origin as the gateway listener (the agent-plane paths are
            # routed there by EndpointAgentRule), so it mirrors
            # ANYRAY_GATEWAY_PUBLIC_URL exactly.
            - Name: ANYRAY_ENDPOINT_CONTROL_PUBLIC_URL
              Value: !If
                - HasGatewayUrl
                - !Ref GatewayPublicUrl
                - !If [HasCert, !Sub 'https://${Alb.DNSName}:8787', !Sub 'http://${Alb.DNSName}:8787']
          Secrets:
            # Same database the gateway's ANYRAY_SPEND_DB_URL points at — the
            # endpoint_* tables coexist there.
            - { Name: ANYRAY_CP_DATABASE_URL, ValueFrom: !GetAtt DbUrlSecret.DbUrlArn }
            # Paired key: the SAME secret the gateway's ANYRAY_ADMIN_TOKEN
            # comes from (the gateway authenticates to this service by falling
            # back to its own admin token) — never a separately minted value,
            # or the pair drifts.
            - { Name: ANYRAY_ENDPOINT_CONTROL_ADMIN_TOKEN, ValueFrom: !GetAtt AppSecrets.AdminTokenArn }
          HealthCheck:
            # /livez is process-only liveness; the ALB target group probes
            # /readyz (Postgres + drain) — the service's documented split.
            Command: [CMD-SHELL, 'node -e "fetch(''http://localhost:8090/livez'').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"']
            Interval: 10
            Timeout: 5
            Retries: 10
            StartPeriod: 20
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref EndpointControlLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: endpoint-control

  ProxyTask:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub '${AWS::StackName}-proxy'
      RequiresCompatibilities: [FARGATE]
      NetworkMode: awsvpc
      Cpu: '256'
      Memory: '512'
      ExecutionRoleArn: !GetAtt ExecutionRole.Arn
      TaskRoleArn: !GetAtt TaskRole.Arn
      ContainerDefinitions:
        - Name: proxy
          Image: !Sub 'public.ecr.aws/anyray/proxy:${ImageTag}'
          Essential: true
          # Stated rather than left implicit, so this is not the one task whose
          # budget nobody chose. 30 (the ECS default) is deliberate here and not
          # merely inherited: unlike Compose, the AWS proxy carries no /v1
          # stream — the gateway has its own ALB listener on 8787 — so it serves
          # only short console requests, and its target group deregisters in
          # 15s. nginx drains its workers on SIGQUIT well inside that.
          StopTimeout: 30
          PortMappings:
            - { Name: proxy, ContainerPort: 80, Protocol: tcp }
          Environment:
            # nginx resolves this at request time via its `resolver` directive
            # (raw DNS, /etc/hosts is never consulted), so it MUST be a name
            # with a real DNS record — the Cloud Map A record registered by
            # GatewayDiscovery. The image default (`gateway`) only works where
            # the platform DNS resolves bare service names (Docker Compose).
            - { Name: ANYRAY_GATEWAY_HOST, Value: !Sub 'gateway-dns.${AWS::StackName}.anyray.internal' }
            # The baked nginx envsubst template needs updater status/token
            # placeholders set. QuickLaunch does not run the Docker/Watchtower
            # updater, so report it disabled.
            - { Name: ANYRAY_UPDATER_ENABLED, Value: 'false' }
            - { Name: ANYRAY_UPDATER_PERIODIC_POLLS, Value: 'false' }
            - { Name: ANYRAY_UPDATER_POLL_INTERVAL, Value: '0' }
            - { Name: ANYRAY_UPDATER_TOKEN, Value: '' }
          HealthCheck:
            Command: [CMD-SHELL, 'wget -qO- http://localhost:80/anyray-login || exit 1']
            Interval: 10
            Timeout: 5
            Retries: 10
            StartPeriod: 20
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref ProxyLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: proxy

  # ---- Services (Cloud Map DNS wires *.anyray.internal hostnames) --------------
  # The V2 logical ids are load-bearing for UPDATES: they force CloudFormation
  # to REPLACE the services (create new, delete old) instead of an in-place
  # Service Connect -> ServiceRegistries transition, which trips ECS deployment
  # hooks on stacks deployed before the DNS fix. Replacement is also what lets
  # the old Service Connect-managed Cloud Map services be torn down cleanly
  # (ECS deletes them with the old services).
  OptimizerServiceV2:
    Type: AWS::ECS::Service
    DependsOn: [EfsMountA, EfsMountB]
    Properties:
      Cluster: !Ref Cluster
      LaunchType: FARGATE
      DesiredCount: !If [Ha, 2, 1]
      # Same hardening the production stack runs: a deployment whose tasks keep
      # failing rolls back automatically instead of hanging the service (and the
      # CloudFormation update fails fast instead of waiting out the full timeout).
      # MinimumHealthyPercent/MaximumPercent are stated rather than left to the
      # ECS default so a rolling deploy provably never dips below the current
      # task count: surge to 200%, wait for the new tasks to pass their target
      # group health check, then retire the old ones.
      DeploymentConfiguration:
        MinimumHealthyPercent: 100
        MaximumPercent: 200
        DeploymentCircuitBreaker: { Enable: true, Rollback: true }
      TaskDefinition: !Ref OptimizerTask
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: ENABLED
          Subnets: [!GetAtt Net.SubnetA, !GetAtt Net.SubnetB]
          SecurityGroups: [!Ref TasksSg]
      ServiceRegistries:
        - RegistryArn: !GetAtt OptimizerDiscovery.Arn

  GatewayServiceV2:
    Type: AWS::ECS::Service
    DependsOn: [GatewayListener, EfsMountA, EfsMountB]
    Properties:
      Cluster: !Ref Cluster
      LaunchType: FARGATE
      DesiredCount: !If [Ha, 2, 1]
      # Same hardening the production stack runs: a deployment whose tasks keep
      # failing rolls back automatically instead of hanging the service (and the
      # CloudFormation update fails fast instead of waiting out the full timeout).
      # MinimumHealthyPercent/MaximumPercent are stated rather than left to the
      # ECS default so a rolling deploy provably never dips below the current
      # task count: surge to 200%, wait for the new tasks to pass their target
      # group health check, then retire the old ones.
      DeploymentConfiguration:
        MinimumHealthyPercent: 100
        MaximumPercent: 200
        DeploymentCircuitBreaker: { Enable: true, Rollback: true }
      TaskDefinition: !Ref GatewayTask
      # Must cover a full migration run. The gateway applies the ordered
      # migration ledger at boot and REFUSES TO SERVE on an unmigrated schema, so
      # a task can sit RUNNING and not yet answering for as long as the
      # migrations take; the ledger allows a single statement 30 minutes, and
      # eight of its migrations are CREATE INDEX CONCURRENTLY over tables holding
      # real rows. At 300s ECS killed and replaced the task mid-migration, the
      # replacement restarted the same migration, and it never converged — the
      # failure lands on the installs least able to absorb it (months of spend
      # data, or a jump across several releases). Costs nothing in detection: the
      # grace only suppresses health-check-driven REPLACEMENT, the ALB still
      # refuses to route to a target that is not passing, and a genuinely broken
      # gateway exits fatally, and a stopped container is replaced regardless.
      HealthCheckGracePeriodSeconds: 1800
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: ENABLED
          Subnets: [!GetAtt Net.SubnetA, !GetAtt Net.SubnetB]
          SecurityGroups: [!Ref TasksSg]
      LoadBalancers:
        - { TargetGroupArn: !Ref GatewayTg, ContainerName: gateway, ContainerPort: 8787 }
      ServiceRegistries:
        - RegistryArn: !GetAtt GatewayDiscovery.Arn

  EndpointControlService:
    Type: AWS::ECS::Service
    DependsOn: [EndpointAgentRule]
    Properties:
      Cluster: !Ref Cluster
      LaunchType: FARGATE
      DesiredCount: !If [Ha, 2, 1]
      # Same hardening the production stack runs: a deployment whose tasks keep
      # failing rolls back automatically instead of hanging the service (and the
      # CloudFormation update fails fast instead of waiting out the full timeout).
      # MinimumHealthyPercent/MaximumPercent are stated rather than left to the
      # ECS default so a rolling deploy provably never dips below the current
      # task count: surge to 200%, wait for the new tasks to pass their target
      # group health check, then retire the old ones.
      DeploymentConfiguration:
        MinimumHealthyPercent: 100
        MaximumPercent: 200
        DeploymentCircuitBreaker: { Enable: true, Rollback: true }
      TaskDefinition: !Ref EndpointControlTask
      HealthCheckGracePeriodSeconds: 120
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: ENABLED
          Subnets: [!GetAtt Net.SubnetA, !GetAtt Net.SubnetB]
          SecurityGroups: [!Ref TasksSg]
      LoadBalancers:
        - { TargetGroupArn: !Ref EndpointControlTg, ContainerName: endpoint-control, ContainerPort: 8090 }
      ServiceRegistries:
        - RegistryArn: !GetAtt EndpointControlDiscovery.Arn

  ProxyServiceV2:
    Type: AWS::ECS::Service
    DependsOn: [ConsoleListener]
    Properties:
      Cluster: !Ref Cluster
      LaunchType: FARGATE
      DesiredCount: !If [Ha, 2, 1]
      # Same hardening the production stack runs: a deployment whose tasks keep
      # failing rolls back automatically instead of hanging the service (and the
      # CloudFormation update fails fast instead of waiting out the full timeout).
      # MinimumHealthyPercent/MaximumPercent are stated rather than left to the
      # ECS default so a rolling deploy provably never dips below the current
      # task count: surge to 200%, wait for the new tasks to pass their target
      # group health check, then retire the old ones.
      DeploymentConfiguration:
        MinimumHealthyPercent: 100
        MaximumPercent: 200
        DeploymentCircuitBreaker: { Enable: true, Rollback: true }
      TaskDefinition: !Ref ProxyTask
      HealthCheckGracePeriodSeconds: 120
      NetworkConfiguration:
        AwsvpcConfiguration:
          AssignPublicIp: ENABLED
          Subnets: [!GetAtt Net.SubnetA, !GetAtt Net.SubnetB]
          SecurityGroups: [!Ref TasksSg]
      LoadBalancers:
        - { TargetGroupArn: !Ref ProxyTg, ContainerName: proxy, ContainerPort: 80 }

Outputs:
  ConsoleURL:
    Description: Open this and sign in with the admin token (see AdminTokenCmd below).
    Value: !If
      - HasConsoleUrl
      - !Ref ConsolePublicUrl
      - !If [HasCert, !Sub 'https://${Alb.DNSName}:3000', !Sub 'http://${Alb.DNSName}:3000']
  GatewayURL:
    Description: Gateway API base URL - point local tools at it with `anyray-connect --gateway <url>`.
    Value: !If
      - HasGatewayUrl
      - !Ref GatewayPublicUrl
      - !If [HasCert, !Sub 'https://${Alb.DNSName}:8787', !Sub 'http://${Alb.DNSName}:8787']
  AdminTokenCmd:
    Description: Fetch the generated admin key from Secrets Manager (it is NOT printed in stack outputs).
    Value: !Sub 'aws secretsmanager get-secret-value --region ${AWS::Region} --secret-id ${AppSecrets.AdminTokenArn} --query SecretString --output text'
  Reachability:
    Description: Where the two URLs above can be opened from.
    Value: !If
      - InternalLb
      - 'PRIVATE: the load balancer is internal, so its DNS name resolves only inside the VPC. Reach it over VPN, Direct Connect, or a peered/attached network - there is no public entry point. Developer machines running anyray-connect need that same access.'
      - 'PUBLIC: the load balancer has public IPs, restricted to AllowedCidr. Set LoadBalancerScheme=internal to keep it entirely inside your VPC.'
  FirstBootNote:
    Description: First boot pulls images, provisions RDS, and seeds prices; allow ~5-10 min. The gateway restarts until RDS is reachable.
    Value: 'Watch the ECS services in the console; the gateway + console go healthy once RDS is up.'
