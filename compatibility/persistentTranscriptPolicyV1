This install revision declares the persistentTranscriptPolicyV1 capability and
wires its coordinated activation into every full-gateway customer template.

Release version stamps do not change this marker. Setup and Railway consume it
directly; CI keeps it aligned with the Helm chart annotation and CloudFormation
metadata. Do not backport it without the Compose, Helm, Railway, and
CloudFormation activation settings.
