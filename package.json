{
  "name": "anyray-install-railway-iac",
  "private": true,
  "description": "Railway Infrastructure-as-Code for the Anyray stack (.railway/railway.ts). Run `npm install` then `railway config apply`.",
  "scripts": {
    "test:desktop-release": "node --test scripts/verify-connect-desktop-*.test.mjs"
  },
  "devDependencies": {
    "railway": "^3.5.2"
  }
}
