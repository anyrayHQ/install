# @@TEMPLATE-ONLY@@ TEMPLATE — do not submit this file. Everything in this block
# @@TEMPLATE-ONLY@@ is stripped by scripts/gen-winget-manifests.sh, which also
# @@TEMPLATE-ONLY@@ substitutes __VERSION__ and __SHA256__ and writes the result
# @@TEMPLATE-ONLY@@ under winget/manifests/a/Anyray/Connect/<version>/. The
# @@TEMPLATE-ONLY@@ __SHA256__ comes from the release's SHA256SUMS line for
# @@TEMPLATE-ONLY@@ anyray-connect-windows-x64.exe; the generator fails loudly
# @@TEMPLATE-ONLY@@ rather than ever emit an empty or placeholder hash.
# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
#
# InstallerType is `portable`: the asset is a bare Bun-compiled console
# executable, NOT an MSI/Inno/NSIS self-installer — it has no install UI and no
# silent-install switches. winget places it in its Links dir and shims it onto
# PATH. The PATH command name comes from the root-level `Commands` list; a bare
# portable exe has no PortableCommandAlias (that field applies only to a portable
# nested inside a zip via NestedInstallerFiles). Without `Commands`, winget would
# derive the alias from the URL file name (anyray-connect-windows-x64).
PackageIdentifier: Anyray.Connect
PackageVersion: __VERSION__
MinimumOSVersion: 10.0.0.0
InstallerType: portable
Commands:
- anyray-connect
Installers:
- Architecture: x64
  InstallerUrl: https://github.com/anyrayHQ/install/releases/download/connect-v__VERSION__/anyray-connect-windows-x64.exe
  InstallerSha256: __SHA256__
ManifestType: installer
ManifestVersion: 1.6.0
